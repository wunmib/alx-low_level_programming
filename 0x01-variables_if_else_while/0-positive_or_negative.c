#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
*main - entry point
*Return: always return 0
*function main - find the last digit of n
*/
int main main (void)
{
int n;
srand(time(0);
n=rand() - RAND_MAX / 2;
if (n < 0)
{
  printf("%d is negative\n",n)
} else if (n == 0)
}
printf("%d is zero\n",n);
}
return (0);
}
