#include <stdio.h>
#include <stdlib.h>
/**
 * Description: main - print out a string to stdout.
 * Return: 0 if successful.
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");

	return (0);
}
